name: CI

on: push

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Build the backend Docker image
        run: docker build -t quotes-backend ./backend